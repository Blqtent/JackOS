class DiskArray {
    constructor Array new(int size) {
        var int s;
		let s = size;
		return Disk.alloc(s);
    }
    method void dispose() {
        do Disk.deAlloc(this);
        return;
    }
	
	method int size() {return s;}
	
}
